#!/bin/bash
# Nexwave Visual Livestream - ASCII Art Dashboard
# Maximum visual appeal for fans and judges

# Color codes
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
MAGENTA='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
BOLD='\033[1m'
NC='\033[0m' # No Color

clear

# Display animated header
show_header() {
    clear
    echo -e "${CYAN}"
    cat << 'HEADER'
╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║    ███╗   ██╗███████╗██╗  ██╗██╗    ██╗ █████╗ ██╗   ██╗███████╗        ║
║    ████╗  ██║██╔════╝╚██╗██╔╝██║    ██║██╔══██╗██║   ██║██╔════╝        ║
║    ██╔██╗ ██║█████╗   ╚███╔╝ ██║ █╗ ██║███████║██║   ██║█████╗          ║
║    ██║╚██╗██║██╔══╝   ██╔██╗ ██║███╗██║██╔══██║╚██╗ ██╔╝██╔══╝          ║
║    ██║ ╚████║███████╗██╔╝ ██╗╚███╔███╔╝██║  ██║ ╚████╔╝ ███████╗        ║
║    ╚═╝  ╚═══╝╚══════╝╚═╝  ╚═╝ ╚══╝╚══╝ ╚═╝  ╚═╝  ╚═══╝  ╚══════╝        ║
║                                                                           ║
║              🤖 AUTONOMOUS TRADING AGENT - LIVE STREAM 🤖                ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝
HEADER
    echo -e "${NC}"
    
    echo -e "${WHITE}${BOLD}"
    echo "┌─────────────────────────────────────────────────────────────────────────┐"
    echo -e "│  ${GREEN}⚡ STRATEGY${NC}${WHITE}${BOLD}      Volume-Weighted Momentum (VWM)                     │"
    echo -e "│  ${YELLOW}💰 PORTFOLIO${NC}${WHITE}${BOLD}     \$159 USDC → \$795-\$1,590 (5x leverage)             │"
    echo -e "│  ${BLUE}📊 PAIRS${NC}${WHITE}${BOLD}         30 perpetuals on Pacifica DEX                      │"
    echo -e "│  ${MAGENTA}🎯 THRESHOLD${NC}${WHITE}${BOLD}     0.1% momentum + 1.2x volume confirmation          │"
    echo -e "│  ${CYAN}🛡️  PROTECTION${NC}${WHITE}${BOLD}    Stop Loss (2.5x ATR) + Take Profit (5x ATR)        │"
    echo "└─────────────────────────────────────────────────────────────────────────┘"
    echo -e "${NC}"
    
    echo -e "${YELLOW}╔═══════════════════════════════════════════════════════════════════════════╗"
    echo -e "║                         📡 LIVE TRADING SIGNALS 📡                        ║"
    echo -e "╚═══════════════════════════════════════════════════════════════════════════╝${NC}"
    echo ""
}

# Show initial header
show_header

# Counter for activity
signal_count=0
order_count=0

# Follow logs with visual enhancements
docker logs nexwave-trading-engine -f --tail 0 2>&1 | grep --line-buffered -E "(BUY signal|SELL signal|Order request sent|STOP LOSS|TAKE PROFIT|confidence|Generated)" | while IFS= read -r line; do
    timestamp=$(date '+%H:%M:%S')
    
    # Parse different event types with ASCII art
    if echo "$line" | grep -q "BUY signal"; then
        ((signal_count++))
        symbol=$(echo "$line" | grep -oP 'for \K[A-Z0-9]+' | head -1)
        vwm=$(echo "$line" | grep -oP 'VWM=\K[0-9.]+' | head -1)
        volume=$(echo "$line" | grep -oP 'Volume Ratio=\K[0-9.]+' | head -1)
        
        echo -e "${GREEN}${BOLD}"
        echo "┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓"
        echo -e "┃  🟢 ${WHITE}BUY SIGNAL DETECTED${GREEN} #${signal_count}                                         ┃"
        echo "┣━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┫"
        echo -e "┃  ${WHITE}Symbol:${GREEN}    ${symbol}                                                        ┃"
        echo -e "┃  ${WHITE}Momentum:${GREEN}  ${vwm}% (bullish)                                          ┃"
        echo -e "┃  ${WHITE}Volume:${GREEN}    ${volume}x average (confirmed)                              ┃"
        echo -e "┃  ${WHITE}Time:${GREEN}      [$timestamp]                                              ┃"
        echo "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛"
        echo -e "${NC}"
        
    elif echo "$line" | grep -q "SELL signal"; then
        ((signal_count++))
        symbol=$(echo "$line" | grep -oP 'for \K[A-Z0-9]+' | head -1)
        vwm=$(echo "$line" | grep -oP 'VWM=\K[0-9.-]+' | head -1)
        volume=$(echo "$line" | grep -oP 'Volume Ratio=\K[0-9.]+' | head -1)
        
        echo -e "${RED}${BOLD}"
        echo "┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓"
        echo -e "┃  🔴 ${WHITE}SELL SIGNAL DETECTED${RED} #${signal_count}                                        ┃"
        echo "┣━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┫"
        echo -e "┃  ${WHITE}Symbol:${RED}    ${symbol}                                                        ┃"
        echo -e "┃  ${WHITE}Momentum:${RED}  ${vwm}% (bearish)                                         ┃"
        echo -e "┃  ${WHITE}Volume:${RED}    ${volume}x average (confirmed)                              ┃"
        echo -e "┃  ${WHITE}Time:${RED}      [$timestamp]                                              ┃"
        echo "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛"
        echo -e "${NC}"
        
    elif echo "$line" | grep -q "Order request sent"; then
        ((order_count++))
        echo -e "${CYAN}${BOLD}"
        echo "    ┌───────────────────────────────────────────────────────────────────┐"
        echo -e "    │  ✅ ${WHITE}ORDER PLACED${CYAN} #${order_count}                                           │"
        echo "    └───────────────────────────────────────────────────────────────────┘"
        echo -e "${NC}"
        
    elif echo "$line" | grep -q "Generated.*signal"; then
        confidence=$(echo "$line" | grep -oP 'confidence=\K[0-9.]+' | head -1)
        amount=$(echo "$line" | grep -oP 'amount=\K[0-9.]+' | head -1)
        price=$(echo "$line" | grep -oP 'price=\$\K[0-9.]+' | head -1)
        
        echo -e "${YELLOW}"
        echo "    │  📊 Confidence: ${confidence}% | Amount: ${amount} | Price: \$${price}"
        echo -e "${NC}"
        
    elif echo "$line" | grep -q "STOP LOSS"; then
        echo -e "${RED}${BOLD}"
        echo "    ╔═══════════════════════════════════════════════════════════════════╗"
        echo -e "    ║  🛑 ${WHITE}STOP LOSS TRIGGERED${RED} - Position closed for protection       ║"
        echo "    ╚═══════════════════════════════════════════════════════════════════╝"
        echo -e "${NC}"
        
    elif echo "$line" | grep -q "TAKE PROFIT"; then
        echo -e "${GREEN}${BOLD}"
        echo "    ╔═══════════════════════════════════════════════════════════════════╗"
        echo -e "    ║  💰 ${WHITE}TAKE PROFIT TRIGGERED${GREEN} - Profits locked in!              ║"
        echo "    ╚═══════════════════════════════════════════════════════════════════╝"
        echo -e "${NC}"
    fi
    
    # Show stats every 10 signals
    if [ $((signal_count % 10)) -eq 0 ] && [ $signal_count -gt 0 ]; then
        echo -e "${MAGENTA}${BOLD}"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo -e "   📈 ${WHITE}SESSION STATS:${MAGENTA} ${signal_count} signals detected | ${order_count} orders placed"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo -e "${NC}"
    fi
done
